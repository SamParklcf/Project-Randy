namespace Randy.ObjectOrientedProgramming.Defenitions
{
    /*
      *  Some useful links:
      *      https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/class
      *      https://www.programiz.com/csharp-programming/class-objects
      */
    /*
     *  C# is an object-oriented program. In object-oriented programming(OOP), we solve complex problems by dividing them into objects.
     *  To work with objects, we need to perform the following activities:
     *      - create a class
     *      - create objects from the class
     */

    /*
     *  Class is the blueprint for the object.
     *  We can think of the class as a sketch (prototype) of a house. 
     *  It contains all the details about the floors, doors, windows, etc. We can build a house based on these descriptions. 
     *  House is the object. 
     *  Like many houses can be made from the sketch, we can create many objects from a class.
     */

    class CLASS
    {
    }

    /*
     *  An object is an instance of a class. Suppose, we have a class Dog. Bulldog, German Shepherd, Pug are objects of the class.
     */

    class Dog
    {

    }

    /*
     *  we use the {new} keyword to create an object of a class
     *      Dong bullDog = new Dog();  => bullDog is an object which created of Dog class. Now, the bullDog object can access the fields and methods of the Dog class.
     */
}
